# legacy

The previous ryukijano.github.io implementation, kept for reference only.
Nothing here is imported by the live app, nothing here is built, and ESLint
ignores this directory. Delete it once the port is settled.

- `App.legacy.jsx` — the single-file React app the site shipped before the rebuild.
- `SpaceBackground.jsx` — the three.js starfield it rendered behind everything.
